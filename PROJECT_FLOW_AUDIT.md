# 照安心 MVP 流程與缺口檢查

檢查日期：2026-08-12

## 目前主要流程

```mermaid
flowchart TD
    A[訪客進入照安心] --> B{登入或建立帳號}
    B -->|建立帳號| C{選擇身份}
    C --> U[使用者／家屬申請]
    C --> P[受照護者申請]
    C --> N[居服員申請與上傳證照]
    B -->|登入| R{後端驗證身份}

    U --> R
    P --> R
    N --> R

    R -->|USER／PATIENT| UD[使用者照護中心]
    R -->|NURSE| NW[居服員工作台]
    R -->|ADMIN| AD[管理控制台]

    UD --> UR[建立或選擇受照護者]
    UR --> UC[瀏覽已認證居服員]
    UC --> UB[提出照護需求]
    UB --> DB[(MongoDB / REST API)]

    DB --> NW
    NW --> NA{居服員判斷是否承接}
    NA -->|確認| BS[確認任務／等待服務]
    NA -->|不承接| BR[退回媒合]
    BS --> SR[報到、安全確認、服務紀錄]
    SR --> SC[完成服務]
    SC --> RV[雙向日誌、星級評價與申訴]
    RV --> DB

    DB --> AD
    AD --> AM[成員與證照管理]
    AD --> AB[預約動向]
    AD --> AQ[品質警訊與使用洞察]

    PS[Pinia 共用同步狀態] -.Socket.IO 即時事件／60 秒備援同步／跨分頁通知.-> UD
    PS -.-> NW
    PS -.-> AD
    DB -.API 資料來源.-> PS
```

## 這次完成的即時同步與安全補強

- Pinia 原本已安裝並啟用，本次沒有重複安裝。
- USER、NURSE、ADMIN 三個主要頁面共用 `live-sync-store`。
- Socket.IO 已連接 USER、NURSE、ADMIN，預約、品質警訊與定位異動會立即要求相關頁面更新。
- 保留每 60 秒重新讀取作為斷線備援；瀏覽器回到前景或網路恢復時立即更新。
- 同一瀏覽器不同分頁完成新增、修改、取消或隱藏後，其他分頁會收到更新通知。
- 網路中斷時保留畫面並停止無效請求，恢復後重新同步。
- GPS 寫入必須明確同意，會驗證經緯度範圍、設定分享期限，並提供停止分享 API；任務完成或取消會清除位置。
- 站內通知已保存送達、失敗、重試次數與已讀欄位，並提供查詢及標記已讀 API。

## 實際驗收結果

- 後端啟動成功：MongoDB 已連線，API 位於 `http://localhost:3000`。
- 前端啟動成功：本次因 9000/9001 被占用，測試位於 `http://localhost:9002`。
- 管理者登入成功，能進入管理控制台。
- 控制台成功讀取 13 位有效成員、2 位受照護者、10 位居服員與 4 筆服務需求。
- 一般使用者建立帳號分支可進入完整申請表，包含受照護者、移位、沐浴、照片與緊急聯絡人資料。
- 前端型別檢查通過；前端與後端建置通過。
- Playwright E2E 已實際通過「建立受照護者 → 預約 → 接單 → 定位同意 → 報到 → 完成 → 一星評價 → 三次一星品質警訊」。
- 前、後端 production dependencies 的 `npm audit` 均為 0 個已知漏洞。
- 手機尺寸的最後一輪瀏覽器自動驗收受到本機安全限制，仍需人工補驗。

## 目前最重要的遺漏

1. **外部通知尚未送出**：站內通知與已讀紀錄已完成；LINE、Email、瀏覽器推播仍需各服務的正式帳號、憑證與失敗重試工作程序。
2. **檔案正式儲存策略**：本機檔名已改為不可預測 UUID；正式上線仍須選定 S3／Cloudinary 等物件儲存，加入病毒掃描、權限與到期網址。
3. **GPS 行動裝置限制**：後端同意、期限與停止分享已完成；iOS／Android 背景定位、電量與使用者拒絕後的前端引導仍需真機驗收。
4. **營運例外流程**：居服員未回應、臨時請假、服務衝突、天災取消、改派與人工介入仍需統一狀態規則。
5. **RWD 與高齡無障礙驗收**：需人工檢查手機上的大字、焦點順序、鍵盤操作、色彩對比、錯誤提示與 44px 以上觸控區。

## 建議下一步（依優先順序）

1. 選定正式物件儲存與 LINE／Email 供應商並提供測試憑證。
2. 補齊營運例外狀態與通知重試規則，再擴充對應 E2E 案例。
3. 用實體 iPhone／Android 完成 GPS、背景限制與高齡無障礙驗收。
